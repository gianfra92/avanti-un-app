import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.avantiunapp.app',
  appName: "Avanti Un'App",
  webDir: 'dist-mobile/avanti-un-app/browser'
};

export default config;
